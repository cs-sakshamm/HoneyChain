# ==============================================================================
# HoneyChain Blockchain - Dependencies & Runtime Requirements
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Runtime & Compiler Environment
# ------------------------------------------------------------------------------
# Node.js: >= 22.7 LTS (with npm >= 10) - toolchain scripts are TypeScript and
#          are executed directly via native type stripping; no build step.
# TypeScript: ^5.5 (type-checking only via `npm run typecheck`, tsc --noEmit)
# Solidity Compiler: ^0.8.20 (hardhat.config.ts configured for 0.8.20+,
#          EVM target "paris" in scripts)
# EVM Network: Ganache CLI / UI (v7.9+) or Hardhat Network (localhost:8545)

# ------------------------------------------------------------------------------
# 2. Package Dependencies (from blockchain/package.json)
# ------------------------------------------------------------------------------
# Core Development & Compilation Framework
hardhat>=3.16.0,<4.0.0

# Smart Contract Interface & Web3 Library
ethers>=6.17.0,<7.0.0

# Standalone Solidity Compiler
solc>=0.8.20,<0.9.0

# Local EVM Blockchain Emulator for Fast Testing
ganache>=7.9.2,<8.0.0

# TypeScript Compiler (dev, typecheck only - noEmit)
typescript>=5.5.0,<6.0.0

# Node.js Type Definitions (dev)
@types/node>=22.0.0,<23.0.0
